"""
Background complexity enumeration.
"""
from enum import Enum, auto

class BackgroundComplexity(Enum):
    """
    Enum for different levels of background complexity.
    """
    SIMPLE = auto()
    MEDIUM = auto()
    COMPLEX = auto()
